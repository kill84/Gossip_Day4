<!DOCTYPE html>
<html>
<head>
<title>The Gossip Project</title>
</head>

<body>

<% if flash[:notice] %>
<div class="alert alert-success">
 Ton gossip a bien été supprimé.
</div>
<% end %>

<div class="row justify-content-center">
<% @gossips.each do |t| %>
    <div class="card m-4 col-sm-12 col-md-4 col-lg-3 text-center">
      <div class="card-body">
        <h5 class="card-title">
     Auteur: <%= t.user[:first_name] %><br/><br/>
        <p class="card-text">
     Potin: <%= t.title %><br/><br/>
     <%= link_to "Lien vers le potin", gossip_path(t.id), :class => "btn btn-secondary"%>  </a>
       </h5>
      </div>
    </div>
    <% end %>
  </div>
</div>



</body>
</html>
